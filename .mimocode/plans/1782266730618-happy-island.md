# LangGraph 1.x 升级检查结果

## 检查范围

`packages/server` 中所有使用 `@langchain/core`、`@langchain/langgraph`、`@langchain/openai` 的代码。

## 安装版本

- `@langchain/core@1.2.1`
- `@langchain/langgraph@1.4.5`
- `@langchain/openai@1.5.3`
- `zod@^4.4.3` (与 `@langchain/core` 兼容，支持 `^3.25.76 || ^4`)

## TypeScript 检查结果

运行 `tsc --noEmit` 后，**langgraph 相关代码无类型错误**。

唯一的错误在 `src/agents/pi.ts:2`，与 langgraph 无关：

```
src/agents/pi.ts(2,10): error TS2305: Module '"@earendil-works/pi-ai"' has no exported member 'getModel'.
```

## LangGraph API 兼容性分析

| 文件 | 用法 | 状态 |
|------|------|------|
| `src/agents/langchain.ts:1` | `ChatOpenAI` from `@langchain/openai` | ✅ 兼容 |
| `src/agents/langchain.ts:2` | `HumanMessage`, `AIMessage`, `SystemMessage`, `ToolMessage` from `@langchain/core/messages` | ✅ 兼容 |
| `src/agents/langchain.ts:3` | `createReactAgent` from `@langchain/langgraph/prebuilt` | ✅ 兼容 (有 deprecation 警告，建议迁移到 `langchain` 包的 `createAgent`) |
| `src/agents/langchain.ts:7` | `DynamicStructuredTool` from `@langchain/core/tools` | ✅ 兼容 |
| `src/routes/chat.ts:37` | `agent.streamEvents()` with `version: "v2"` | ✅ 兼容 |

## 建议修复

### 1. 修复 `pi.ts` 类型错误 (非 langgraph 相关)

`src/agents/pi.ts:2` — `getModel` 已从 `@earendil-works/pi-ai` 中移除或重命名。需要检查该包的当前导出并更新导入。

### 2. 可选：清理未使用代码

`src/agents/langchain.ts:39-57` 的 `messagesToLangChain` 函数从未被调用，是死代码。

## 验证步骤

1. `cd packages/server && bun run typecheck` — 确认类型检查通过
2. `cd packages/server && bun run dev` — 确认服务启动正常
3. 调用 `/api/chat/langgraph` 端点测试流式对话功能
